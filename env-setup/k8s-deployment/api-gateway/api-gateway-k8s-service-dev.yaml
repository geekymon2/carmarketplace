apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: carmarketplace-dev
spec:
  selector:
    app: api-gateway
  ports:
    - name: http
      protocol: TCP
      port: 8008
      targetPort: 8008